{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "drivnbd/wsgi.py": {
      "runtime": "python3.12",
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/drivnbd/wsgi.py" }],
  "buildCommand": "python -m pip install -U pip && pip install -r requirements.txt && python manage.py collectstatic --noinput"
}
